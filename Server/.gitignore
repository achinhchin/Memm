# secrets & local config
.env
*.local

# TLS material
certs/*.pem
certs/*.key
certs/*.crt

# blob storage & uploads
data/
!data/.gitkeep

# build output (anchored: a bare 'memm' would also exclude cmd/memm/)
/bin/
/memm
*.test
*.out

# os / editor
.DS_Store
.idea/
.vscode/
